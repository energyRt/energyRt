% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-horizon.R
\name{newHorizon}
\alias{newHorizon}
\alias{update,horizon-method}
\title{Create a new object of class 'horizon'}
\usage{
newHorizon(
  period = NULL,
  intervals = NULL,
  mid_is_end = FALSE,
  mid_is_start = FALSE,
  force_BY_interval_to_1_year = T,
  desc = NULL,
  name = NULL
)

\S4method{update}{horizon}(object, ..., warn_nodata = TRUE)
}
\arguments{
\item{period}{(optional) integer vector with a range or a sequence of years to define
the full period of the model/scenario. If not provided, the range of 'intervals' will be used.}

\item{intervals}{(optional) either data.frame or integer vector.
The data.frame must have \code{start}, \code{mid}, and \code{end} columns with modeled interval.
The vector will be considered as lengths of each modeled interval in period.}

\item{mid_is_end}{logical, if TRUE, the mid-year will be set to the end of the interval.}

\item{mid_is_start}{logical, if TRUE, the mid-year will be set to the start of the interval.}

\item{force_BY_interval_to_1_year}{logical, if TRUE (default), the base-year (first) interval will be forced to one year.}

\item{desc}{character. Description of the horizon object, for own references.}

\item{name}{character. Name of the horizon object. Used to distinguish between different horizons in the model or scenario, including the automatic creation of the folder name for the model/scenario scripts.}
}
\value{
An object of class 'horizon'
}
\description{
The function creates a new object of class 'horizon' that represents the planning horizon of the model/scenario.
}
\examples{
newHorizon(2020:2050)
newHorizon(2020:2030, desc = "One-year intervals")
newHorizon(2020:2030, c(1, 2, 5, 10), desc = "Different length intervals")
newHorizon(2020:2035, c(1, 2, 5, 5, 5))
newHorizon(2020:2050, c(1, 2, 5, 7, 1))

newHorizon(intervals = data.frame(
  start = c(2030, 2031, 2034),
  mid =   c(2030, 2032, 2037),
  end =   c(2030, 2033, 2040)),
  desc = "Explicit assignment of intervals via data.frame"
  )

newHorizon(period = 2020:2050,
           intervals = data.frame(
             start = c(2030, 2031, 2034),
             mid =   c(2030, 2032, 2037),
             end =   c(2030, 2033, 2040)),
             desc = "The period will be trimmed to the scope of intervals")

newHorizon(2020:2050, c(3, 2, 5, 10),
           desc = "Pay attention to the length of the first interval")

newHorizon(period = 2020:2040,
           intervals = data.frame(
             start = c(2030, 2032, 2035),
             mid =   c(2031, 2033, 2037),
             end =   c(2032, 2034, 2040)))
}
\seealso{
Other horizon, config, settings, model, scenario: 
\code{\link{horizon-class}}
}
\concept{horizon, config, settings, model, scenario}
\concept{update horizon}
