% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-calendar.R
\name{make_timeslices_table}
\alias{make_timeslices_table}
\title{Create table of time-slices from given structure as a list}
\usage{
make_timeslices_table(struct = list(ANNUAL = "ANNUAL"), warn = FALSE)
}
\arguments{
\item{struct}{named list of timeslices levels with sets and optional shares of levels}

\item{warn}{logical, if TRUE, warning will be issued if `ANNUAL` level does not exists in the given structure. The level will be auto-created to complete the time-structure.}
}
\description{
Create table of time-slices from given structure as a list
}
\examples{
make_timeslices_table()
make_timeslices_table(list("SEASON" = c("WINTER", "SUMMER")))
make_timeslices_table(list("SEASON" = c("WINTER" = .6, "SUMMER" = .4)))
make_timeslices_table(list(
  "SEASON" = list(
    "WINTER" = list(.3, DAY = c("MORNING", "EVENING")),
    "SUMMER" = list(.7, DAY = c("MORNING", "EVENING"))
  )
))

make_timeslices_table(list(
  "SEASON" = list("WINTER" = .3, "SUMMER" = .7),
  "DAY" = c("MORNING", "EVENING")
))

}
